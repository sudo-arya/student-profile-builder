# Student assets

Place profile images, a CV, and other student-owned files here. Reference them from
`profile.md` with relative paths such as `assets/photo.jpg`. Paths outside this folder are rejected.
