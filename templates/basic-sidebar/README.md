# Basic Sidebar

Reference Jinja architecture for a single generated page with desktop sidebar, mobile horizontal navigation, and section anchors. Output is `index.html` plus template assets. Theme switching is intentionally not declared. Every visible normalized section is rendered, including custom types. TAs may replace the CSS and navigation while preserving relative paths and arbitrary-section iteration.
